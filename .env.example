# Local development defaults. Copy this file to .env and change values locally.
FINLEDGER_ENV=development
FINLEDGER_HOST=127.0.0.1
FINLEDGER_PORT=8765
FINLEDGER_DATA_DIR=./data

# Never put real bank credentials or financial data in this public repository.
# Provider-specific variables will be documented when read-only adapters exist.
